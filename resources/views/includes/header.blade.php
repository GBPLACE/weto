<!--// Header \\--><header id="weto-header" class="weto-header-one">    <div class="container">        <div class="row">            <div class="col-md-4">                <a href="{{route('index')}}" class="logo"><img src="{{asset('propertyUploads/'.$homeContent[0]['header_logo'])}}" alt=""></a>            </div>            <div class="col-md-8">                <nav class="navbar navbar-expand-lg">                    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">                        <span class="fa fa-bars"></span>                    </button>                    <div class="collapse navbar-collapse" id="navbarSupportedContent">                        <ul class="navbar-nav mr-auto">                            @if((!session()->has('partner')))                            <li class="nav-item">                                <a class="nav-link" href="{{route('becomeHost')}}">BECOME A HOST</a>                            </li>                            @endif                            <li class="nav-item">                                <a class="nav-link" href="{{route('search')}}">SEARCH</a>                            </li>                            @if((!session()->has('partner')) && (!session()->has('user')))                            <li class="nav-item">                                <a class="nav-link" href="{{route('login')}}">LOG IN</a>                            </li>                            @endif                        </ul>                    </div>                </nav>                @if((session()->has('partner')))                <div class="profile dropdown">                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">                        {{session('partner')['name']}}                    </a>                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">                        <a class="dropdown-item" href="{{route('partnerPropertyList')}}">View Property List</a>                        <a class="dropdown-item" href="{{route('addProperty')}}">Add New Property</a>                        <a class="dropdown-item" href="{{route('draft')}}">Drafts</a>                        <a class="dropdown-item" href="{{route('blockedProperties')}}">Blocked Properties</a>                        <div class="dropdown-divider"></div>                        <a class="dropdown-item" href="{{route('settings')}}">Settings</a>                        <a class="dropdown-item" href="{{route('logout')}}">Logout</a>                    </div>                </div>                @endif                @if((session()->has('user')))                    <div class="profile dropdown">                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">                            {{session('user')['name']}}                        </a>                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">                            <a class="dropdown-item" href="{{route('wishList')}}">Wishlist</a>                            <div class="dropdown-divider"></div>                            <a class="dropdown-item" href="{{route('userSettings')}}">Settings</a>                            <a class="dropdown-item" href="{{route('logout')}}">Logout</a>                        </div>                    </div>                @endif            </div>        </div>    </div></header><!--// Header \\-->